version: '3.8'
services:
  intelligence-hub:
    build: .
    container_name: intelligence-hub
    ports:
      - "8080:80"
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./data:/app/data
    restart: always
